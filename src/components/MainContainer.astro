---
const { class: className, id } = Astro.props
---

<main
  id={id}
  data-section={id}
  class={`main ${className} p-4 scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</main>
